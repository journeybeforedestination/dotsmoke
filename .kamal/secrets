# Kamal resolves the secret names config/deploy.yml refers to from this file. No
# credential is written here and none should be: the value comes from the environment of
# whatever runs the deploy, which in CI is the GITHUB_TOKEN minted for that workflow run
# and thrown away when it ends.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
